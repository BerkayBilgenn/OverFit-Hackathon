name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Motor ve sözleşme testleri
        run: npm test

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Katalog üretici testleri
        run: python3 -m unittest tests.test_build_program_catalog -v
